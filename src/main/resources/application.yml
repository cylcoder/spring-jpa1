spring:
  application:
    name: demo

  datasource:
    url: jdbc:h2:tcp://localhost/~/Development/h2/databases/spring-jpa1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#        show_sql: true # System.out? SQL ?? ??
#        format_sql: true
#
#logging:
#  level:
#    org.hibernate.sql: debug # logger? SQL ?? ??